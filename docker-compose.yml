version: "3"
services:
  doccano:
#    image: chakkiworks/doccano
    build:
       context: .
       dockerfile: Dockerfile
    ports:
      - '50010:80'
    volumes:
      - doccano:/doccano

volumes:
  doccano: